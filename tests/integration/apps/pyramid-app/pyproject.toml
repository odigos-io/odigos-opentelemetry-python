[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pyramid-app"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "pyramid==2.0.2",
    "waitress==3.0.2",
]

[tool.setuptools]
py-modules = []
