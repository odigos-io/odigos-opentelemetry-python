[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[project]
name = "flask-app"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "flask==3.1.0",
]

[tool.setuptools]
py-modules = []
