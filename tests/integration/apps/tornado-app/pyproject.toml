[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tornado-app"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "tornado==6.4.2",
]

[tool.setuptools]
py-modules = []
