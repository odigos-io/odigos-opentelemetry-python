[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[project]
name = "django-app"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "django==4.2.17",
    "gunicorn==23.0.0",
]

[tool.setuptools]
py-modules = []
