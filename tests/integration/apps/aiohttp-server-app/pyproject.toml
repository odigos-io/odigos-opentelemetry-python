[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[project]
name = "aiohttp-server-app"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "aiohttp==3.11.12",
]

[tool.setuptools]
py-modules = []
