[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[project]
name = "falcon-app"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "falcon==3.1.3",
    "waitress==3.0.2",
]

[tool.setuptools]
py-modules = []
